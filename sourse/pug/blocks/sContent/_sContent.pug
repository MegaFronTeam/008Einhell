mixin sContent()
	// start sContent
	+b.SECTION.sContent.section#sContent&attributes(attributes)
		.container
			if block
				block
			else
				+b.section-title.text-center
					h2 sContent
					
				+e.row.row
	// end sContent